#Install nodejs, npm, react
sudo apt update
sudo apt upgrade
sudo apt install nodejs
sudo apt install npm
sudo apt install build-essential
npm install -g react-native-cli

#To start making apps
create-react-native-app <NewAppName>

#Make sure your phone 'expo' app is ready! (same network too!)
sudo npm test <ORRRRRRR> sudo expo test

#Now edit at your own leisure!-real time saving
#Use the App.js file as the pseudo 'index'
#figure out the event driven stiff for
	#...willmount?
	#render()?
	#dismount?
	#etc...
